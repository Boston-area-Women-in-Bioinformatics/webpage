---
interface Props {
  artists: string[];
}

const { artists } = Astro.props;
---

{
  artists.map((txtblock) => {
    return (
      <>
        <div class="mb-2">{txtblock}</div>
      </>
    );
  })
}
