---
/**
 * PostListItem Component
 * A reusable list item for displaying posts (blog posts, newsletters, etc.)
 *
 * @param post - The post to display (Post or Newsletter type)
 */

import { Icon } from 'astro-icon/components';
import Image from '~/components/common/Image.astro';
import PostTags from '~/components/blog/Tags.astro';

import { APP_BLOG, APP_NEWSLETTER } from 'astrowind:config';
import type { Post, Newsletter } from '~/types';

import { getPermalink } from '~/utils/permalinks';
import { findImage } from '~/utils/images';
import { getFormattedDate } from '~/utils/utils';

export interface Props {
  post: Post | Newsletter;
}

const { post } = Astro.props;
const image = await findImage(post.image);

// Check if image is a remote/public path (string starting with http, /assets/, /blog_images/, or /photos/)
const isRemoteImage =
  typeof image === 'string' &&
  (image.startsWith('http') ||
    image.startsWith('/assets/') ||
    image.startsWith('/blog_images/') ||
    image.startsWith('/photos/'));

// Determine if this is a blog post or newsletter
const isBlogPost = 'tags' in post;
const isNewsletter = 'issue' in post;
const config = isBlogPost ? APP_BLOG : APP_NEWSLETTER;
const link = config?.post?.isEnabled ? getPermalink(post.permalink, 'post') : '';

// Use imageAlt if provided, otherwise fall back to title for accessibility
const altText = post.imageAlt || post.title;

// Determine object position based on imagePosition field
const getObjectPosition = (position?: 'top' | 'center' | 'bottom') => {
  switch (position) {
    case 'top':
      return 'top';
    case 'bottom':
      return 'bottom';
    case 'center':
    default:
      return 'center';
  }
};

const objectPosition = getObjectPosition(post.imagePosition);
---

<article
  class={`max-w-md mx-auto md:max-w-none grid gap-6 md:gap-8 intersect-once intersect-quarter motion-safe:md:opacity-0 motion-safe:md:intersect:animate-fade ${image ? 'md:grid-cols-2' : ''}`}
>
  {
    image &&
      (link ? (
        <a class="relative block group" href={link ?? 'javascript:void(0)'}>
          <div class="relative h-0 pb-[56.25%] overflow-hidden bg-gray-400 dark:bg-slate-700 rounded shadow-lg">
            {isRemoteImage ? (
              <img
                src={image}
                class="absolute inset-0 object-cover w-full h-full mb-6 rounded shadow-lg bg-gray-400 dark:bg-slate-700"
                style={`object-position: ${objectPosition};`}
                alt={altText}
                width="900"
                height="506"
                loading="lazy"
                decoding="async"
              />
            ) : (
              <Image
                src={image}
                class="absolute inset-0 object-cover w-full h-full mb-6 rounded shadow-lg bg-gray-400 dark:bg-slate-700"
                style={`object-position: ${objectPosition};`}
                widths={[400, 900]}
                width={900}
                sizes="(max-width: 900px) 400px, 900px"
                alt={altText}
                aspectRatio="16:9"
                loading="lazy"
                decoding="async"
              />
            )}
          </div>
        </a>
      ) : (
        <div class="relative h-0 pb-[56.25%] overflow-hidden bg-gray-400 dark:bg-slate-700 rounded shadow-lg">
          {isRemoteImage ? (
            <img
              src={image}
              class="absolute inset-0 object-cover w-full h-full mb-6 rounded shadow-lg bg-gray-400 dark:bg-slate-700"
              style={`object-position: ${objectPosition};`}
              alt={altText}
              width="900"
              height="506"
              loading="lazy"
              decoding="async"
            />
          ) : (
            <Image
              src={image}
              class="absolute inset-0 object-cover w-full h-full mb-6 rounded shadow-lg bg-gray-400 dark:bg-slate-700"
              style={`object-position: ${objectPosition};`}
              widths={[400, 900]}
              width={900}
              sizes="(max-width: 900px) 400px, 900px"
              alt={altText}
              aspectRatio="16:9"
              loading="lazy"
              decoding="async"
            />
          )}
        </div>
      ))
  }
  <div class="mt-2">
    <header>
      <div class="mb-1">
        <span class="text-sm text-gray-700 dark:text-slate-300">
          {
            isNewsletter && (
              <>
                <Icon name="tabler:news" class="w-4 h-4 inline-block -mt-0.5 dark:text-gray-400" /> Issue-
                {String((post as Newsletter).issue).padStart(3, '0')} ·{' '}
              </>
            )
          }
          <Icon name="tabler:clock" class="w-3.5 h-3.5 inline-block -mt-0.5 dark:text-gray-400" />
          <time datetime={String(post.publishDate)} class="inline-block">{getFormattedDate(post.publishDate)}</time>
          {
            post.authors?.length && (
              <>
                {' '}
                ·{' '}
                <Icon
                  name={post.authors.length > 1 ? 'tabler:users' : 'tabler:user'}
                  class="w-3.5 h-3.5 inline-block -mt-0.5 dark:text-gray-400"
                />
                {post.authors.map((author, index) => (
                  <Fragment key={index}>
                    {index > 0 && ', '}
                    {author.url ? (
                      <>
                        <a href={author.url} class="hover:underline">
                          <span>{author.name.replaceAll('-', ' ')}</span>
                        </a>
                      </>
                    ) : (
                      <>
                        <Icon name="tabler:user" class="w-3.5 h-3.5 inline-block -mt-0.5 dark:text-gray-400" />
                        <span>{author.name.replaceAll('-', ' ')}</span>
                      </>
                    )}
                  </Fragment>
                ))}
              </>
            )
          }

          {
            isBlogPost &&
              (post as Post).category &&
              (() => {
                const category = (post as Post).category!;
                return (
                  <>
                    {' '}
                    ·{' '}
                    <a class="hover:underline" href={getPermalink(category.slug, 'category')}>
                      {category.title}
                    </a>
                  </>
                );
              })()
          }
        </span>
      </div>
      <h2 class="text-xl sm:text-2xl font-bold leading-tight mb-2 font-heading text-gray-900 dark:text-slate-100">
        {
          link ? (
            <a
              class="inline-block hover:text-primary dark:hover:text-blue-700 transition ease-in duration-200"
              href={link}
            >
              {post.title}
            </a>
          ) : (
            post.title
          )
        }
      </h2>
    </header>

    {post.excerpt && <p class="flex-grow text-muted dark:text-slate-300 text-lg">{post.excerpt}</p>}
    {
      isBlogPost && (post as Post).tags && Array.isArray((post as Post).tags) ? (
        <footer class="mt-5">
          <PostTags tags={(post as Post).tags} />
        </footer>
      ) : (
        <Fragment />
      )
    }
  </div>
</article>
