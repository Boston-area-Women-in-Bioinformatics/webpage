---
interface Props {
  artists: string[];
}

const { artists } = Astro.props;
---

{
  artists.map((artist, index) => {
    return (
      <>
        {artist}
        <span class="text-gray-300">{index === artists.length - 1 ? ' ' : ', '}</span>
      </>
    );
  })
}
