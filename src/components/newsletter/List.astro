---
import Item from '~/components/newsletter/ListItem.astro';
import type { Newsletter } from '~/types';

export interface Props {
  posts: Array<Newsletter>;
}

const { posts } = Astro.props;
---

<ul>
  {
    posts.map((post) => (
      <li class="mb-12 md:mb-20">
        <Item post={post} />
      </li>
    ))
  }
</ul>
